# TypeScript Protocol Bindings

`@nous-wave/protocol` contains generated TypeScript bindings for the Protobuf source under [`proto/`](../../proto/). The `.proto` files, not generated output, own the wire contract.

Regenerate with `corepack pnpm generate` and validate with `corepack pnpm proto:check`.
